# >>> ELF managed >>>
# 이 블록은 `elf update`가 관리합니다 — 직접 수정하지 마세요.
# 사용자 규칙은 블록 아래(사용자 영역)에 추가하세요.

# === Claude Code & AI Toolkit ===
# (CLAUDE.md는 ELF 진입 포인터라 추적 대상 — clone에도 유지되어야 AGENTS.md가 로드됨)
# (.claude/settings.json의 autoread 훅은 비추적 파생물 — clone 후 `elf update`가 재생성)
.claude/
.ecc/
.claudeignore

# === ELF runtime state (autoread 재구성 마커 등 — 세션 한정·비추적) ===
.elf/runtime/

# === MATLAB Autosave files (자동 저장 파일) ===
*.asv
*.m~
*.mlapp~

# === Compiled binaries / MEX files (컴파일된 바이너리) ===
*.mex*
*.mexa64
*.mexw64
*.mexw32
*.mexmaci64

# === Simulink generated files and caches (시뮬링크 캐시 및 빌드 폴더) ===
slprj/
sfprj/
*.slxc

# === MATLAB specific temporary files ===
*.prj~
*.log

# === Office autosave files (자동 저장 임시 파일) ===
*.tmp
~$*.xls*
~$*.ppt*
~$*.doc*
~*.doc*

# === HWP (Hangul) autosave files (한글 자동 저장) ===
~$*.hwp*
~$*.hwpx*
*.hwp.bak
*.hwpx.bak

# === Raw Data (원본 센서 데이터 — 절대 Git 추적 제외) ===
6_Exp/62_Empirical/Raw/
# <<< ELF managed <<<

# === 사용자 영역 (ELF가 건드리지 않습니다) ===
# (선택) 용량이 큰 데이터 파일 제외 — 필요 시 아래 주석 해제
# *.mat
# *.fig
